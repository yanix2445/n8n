# =============================================================================
# 🔐 FICHIER .ENV.EXAMPLE - Template de configuration
# =============================================================================
#
# 📖 INSTRUCTIONS :
# 1. Copiez ce fichier vers .env : cp .env.example .env
# 2. Remplacez TOUTES les valeurs par vos vraies données
# 3. Ne commitez JAMAIS le fichier .env sur Git !
#
# =============================================================================

# =============================================================================
# 🗄️  POSTGRESQL - Configuration de la base de données
# =============================================================================
POSTGRES_DB=n8n_database
POSTGRES_USER=n8n_user
# ⚠️ CHANGEZ ce mot de passe ! Utilisez : openssl rand -base64 32
POSTGRES_PASSWORD=CHANGEZ_MOI_POSTGRES_PASSWORD

# =============================================================================
# 🤖 N8N - Configuration de l'application
# =============================================================================

# Authentification n8n (ce que VOUS tapez pour vous connecter)
N8N_BASIC_AUTH_USER=admin
# ⚠️ CHANGEZ ce mot de passe ! Mettez quelque chose que vous retenez
N8N_BASIC_AUTH_PASSWORD=CHANGEZ_MOI_N8N_PASSWORD

# Clé de chiffrement pour les données sensibles (workflows avec API keys, etc.)
# ⚠️ GÉNÉREZ UNE CLÉ UNIQUE ! Utilisez : openssl rand -hex 32
N8N_ENCRYPTION_KEY=CHANGEZ_MOI_ENCRYPTION_KEY_32_CARACTERES_MINIMUM

# URLs et domaines - remplacez par votre vrai domaine
N8N_HOST=localhost
N8N_PORT=5678
WEBHOOK_URL=https://VOTRE-DOMAINE.com
N8N_EDITOR_BASE_URL=https://VOTRE-DOMAINE.com

# Fuseau horaire
TIMEZONE=Europe/Paris

# =============================================================================
# 🌐 CLOUDFLARE TUNNEL - Configuration HTTPS
# =============================================================================

# Token du tunnel Cloudflare
# ⚠️ À récupérer depuis votre dashboard Cloudflare Zero Trust > Access > Tunnels
# Exemple : eyJhIjoiYWJjZGVmZ2hpams...
CLOUDFLARE_TUNNEL_TOKEN=CHANGEZ_MOI_CLOUDFLARE_TOKEN

# Domaine configuré dans Cloudflare (SANS https://)
# Exemple : n8n.votre-domaine.com
CLOUDFLARE_DOMAIN=VOTRE-DOMAINE.com

# =============================================================================
# 📝 GUIDE DE CONFIGURATION CLOUDFLARE TUNNEL
# =============================================================================
#
# 1. Connectez-vous au dashboard Cloudflare
# 2. Allez dans "Zero Trust" > "Networks" > "Tunnels"
# 3. Créez un nouveau tunnel (nom : n8n)
# 4. Dans la configuration du tunnel :
#    - Type : HTTPS
#    - URL : http://n8n:5678 (⚠️ IMPORTANT : pas localhost !)
#    - Domaine : votre-domaine.com
# 5. Copiez le token généré dans CLOUDFLARE_TUNNEL_TOKEN ci-dessus
# 6. Mettez votre domaine dans CLOUDFLARE_DOMAIN ci-dessus
#
# =============================================================================

# =============================================================================
# 🔧 COMMANDES POUR GÉNÉRER DES MOTS DE PASSE SÉCURISÉS
# =============================================================================
#
# Mot de passe PostgreSQL :
# openssl rand -base64 32
#
# Clé de chiffrement n8n :
# openssl rand -hex 32
#
# Mot de passe n8n (ou utilisez ce que vous voulez retenir) :
# openssl rand -base64 16
#
# =============================================================================